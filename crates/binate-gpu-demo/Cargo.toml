[package]
name = "binate-gpu-demo"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "binate-gpu-demo"
path = "src/main.rs"

[dependencies]
binate-gpu-core     = { path = "../binate-gpu-core" }
binate-gpu-platform = { path = "../binate-gpu-platform" }
