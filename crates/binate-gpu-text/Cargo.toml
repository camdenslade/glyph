[package]
name = "binate-gpu-text"
version = "0.1.0"
edition = "2021"

[dependencies]
binate-gpu-core = { path = "../binate-gpu-core" }
cosmic-text = "0.12"
wgpu = "22"
bytemuck = { version = "1", features = ["derive"] }
