[package]
name = "glyph-text"
version = "0.1.0"
edition = "2021"

[dependencies]
glyph-core = { path = "../glyph-core" }
cosmic-text = "0.12"
wgpu = "22"
bytemuck = { version = "1", features = ["derive"] }
