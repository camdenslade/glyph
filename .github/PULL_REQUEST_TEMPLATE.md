**What does this PR do?**


**Related issue** (if any)

Closes #

**Checklist**
- [ ] `cargo test --workspace --exclude github-glyph` passes
- [ ] `cargo clippy --workspace --exclude github-glyph -- -D warnings` passes
- [ ] No new decorated comment headers (`// ──`)
- [ ] No banner-style comment blocks (`// ---`)
